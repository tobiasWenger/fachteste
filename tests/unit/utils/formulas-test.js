import formulas from 'fachteste/utils/formulas';
import { module, test } from 'qunit';

module('Unit | Utility | formulas');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formulas();
  assert.ok(result);
});


import { toPrecision } from 'fachteste/helpers/to-precision';
import { module, test } from 'qunit';

module('Unit | Helper | to precision');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toPrecision([42]);
  assert.ok(result);
});

